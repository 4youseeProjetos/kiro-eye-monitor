[project]
name = "kiro-eye-monitor"
dynamic = ["version"]
description = "Monitor de consumo de creditos Kiro (conta + sessoes locais do kiro-cli)"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
kiro-eye-collect = "kiro_eye_monitor.main:main"

[dependency-groups]
dev = ["pytest==8.3.4"]

[build-system]
requires = ["hatchling==1.27.0"]
build-backend = "hatchling.build"

# Uma versao so no projeto: o build a le do __version__ em vez de repetir aqui.
[tool.hatch.version]
path = "src/kiro_eye_monitor/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/kiro_eye_monitor"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
